package com.taobao.terminator.common.hsftest;

import com.taobao.config.client.Publisher;
import com.taobao.config.client.PublisherRegistrar;
import com.taobao.config.client.PublisherRegistration;

public class ConfigServerTest2 {
	public static void main(String[] args) {
		PublisherRegistration<String> registration = new PublisherRegistration<String>("yusen", "search4tag-0-innserservice");
		registration.setGroup("Terminator");
		Publisher<String> publisher =  PublisherRegistrar.register(registration);
		publisher.publish("192.168.211.29");
	}
}
